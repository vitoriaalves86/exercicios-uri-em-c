#include <stdio.h>
int main (){

    int n, i, quantia;
    scanf("%d", &n);
    int rato, sapo, coelho;
    char animal;

    for(i=0;i<=n;i++){
        scanf("%d %s", &quantia, &animal);
        resultado = strcmp(animal, 'c');
        if(resultado == 0)
    }


return 0;
}
